# cert-manager — upstream jetstack chart. For AKS we set workload-identity
# SA annotations so cert-manager pods federate into the UAI that TF created.
apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: cert-manager
  namespace: argocd
  annotations:
    argocd.argoproj.io/sync-wave: "-1"
  finalizers:
    - resources-finalizer.argocd.argoproj.io
spec:
  project: trakrf
  source:
    repoURL: https://charts.jetstack.io
    chart: cert-manager
    targetRevision: "v1.16.*"
    helm:
      values: |
        crds:
          enabled: true
        replicaCount: 2
        podDisruptionBudget:
          enabled: true
        {{- if eq .Values.cluster "aks" }}
        serviceAccount:
          labels:
            azure.workload.identity/use: "true"
          annotations:
            azure.workload.identity/client-id: {{ .Values.certManagerIdentityClientId | quote }}
        podLabels:
          azure.workload.identity/use: "true"
        {{- end }}
  destination:
    server: {{ .Values.destination.server }}
    namespace: {{ .Values.namespaces.certManager }}
  # cainjector populates the caBundle field at runtime on its own webhook
  # configurations — that's expected operator behavior, not drift.
  # Ignore fields owned by the cainjector field manager rather than trying to
  # enumerate every jsonPointer.
  ignoreDifferences:
    - group: admissionregistration.k8s.io
      kind: ValidatingWebhookConfiguration
      name: cert-manager-webhook
      managedFieldsManagers:
        - cert-manager-cainjector
    - group: admissionregistration.k8s.io
      kind: MutatingWebhookConfiguration
      name: cert-manager-webhook
      managedFieldsManagers:
        - cert-manager-cainjector
  syncPolicy:
    automated:
      prune: true
      selfHeal: true
    syncOptions:
      - CreateNamespace=true
      - ServerSideApply=true
