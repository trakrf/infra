apiVersion: traefik.io/v1alpha1
kind: TLSStore
metadata:
  name: {{ .Values.tlsStore.name }}
  namespace: {{ .Values.tlsStore.namespace }}
spec:
  defaultCertificate:
    secretName: {{ .Values.tlsStore.defaultCertificateSecret }}
