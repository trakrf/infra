# AKS overrides for trakrf-backend.
# Tag is currently pinned to an amd64-only sha (TRA-451 has not yet shipped
# the multi-arch CI fix). Deployment will apply successfully, pods will
# ImagePullBackOff on the arm64 primary node with "no matching manifest
# for linux/arm64" until TRA-451 merges and we bump to a multi-arch tag.

image:
  tag: sha-9f22fac

ingress:
  host: aks.trakrf.app
