# AKS overrides for trakrf-backend.
# Pinned to the first multi-arch tag published post-TRA-451 (sha-dfb5841
# is the trakrf/platform merge commit of PR #187 that added linux/arm64
# to the docker-build workflow matrix).

image:
  tag: sha-dfb5841

ingress:
  host: aks.trakrf.app
