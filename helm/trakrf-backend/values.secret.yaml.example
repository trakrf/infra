# Copy to values.secret.yaml (gitignored) and fill in real values.
# Usage: helm upgrade trakrf-backend helm/trakrf-backend -f values.secret.yaml
secrets:
  jwtSecret: ""          # openssl rand -base64 48
  resendApiKey: ""       # re_xxx (optional)
  sentryDsn: ""          # https://...@sentry.io/... (optional)
