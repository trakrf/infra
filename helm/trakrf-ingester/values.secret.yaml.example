# Copy to values.secret.yaml (gitignored) and fill in real values.
# Usage: helm upgrade trakrf-ingester helm/trakrf-ingester -f values.secret.yaml
mqtt:
  url: "mqtts://USER:PASSWORD@BROKER.emqxcloud.com:8883"
