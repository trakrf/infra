# AKS overlay — distinct MQTT clientId so AKS doesn't collide with Railway
# prod or other cluster ingesters on the shared EMQX Cloud broker. See
# values-gke.yaml for rationale.
mqtt:
  clientId: trakrf-ingester-aks
