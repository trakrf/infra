# EKS overlay — no cluster-specific overrides for trakrf-ingester today.
# Ingester runs entirely in-cluster (MQTT in, CNPG out via trakrf-db-rw
# service, same hostname on any cluster). Scaffold kept for uniform
# valueFiles references from argocd/root.
